# Antigravity プロジェクト開発ルール

## 画像・アセット管理
- ドキュメントで使用する画像ファイル（`.png`, `.jpg`, `.jpeg`, `.gif`, `.svg` 等）は、プロジェクトルートに置かず必ず `images/` ディレクトリ内に配置すること。
- `README.md` 等のMarkdownからの画像参照は `images/<filename>` の相対パスを使用すること。
