# ドキュメントおよびアセット配置ルール

## 画像ファイルの配置
- `README.md` やその他のドキュメントで使用する画像ファイル（`.png`, `.jpg`, `.jpeg`, `.gif`, `.svg` 等）は、プロジェクトルートに直接置かず、必ず `images/` ディレクトリ内に配置すること。
- Markdownファイル内での画像リンクは、`images/<filename>` の相対パスで記述すること。
